{
  "name": "deal-pop-scraper",
  "version": "1.0.0",
  "description": "DealPop Price Checker Service - Automated price monitoring and alert system for the DealPop ecosystem. Provides scheduled web scraping, price tracking, and multi-channel notifications.",
  "type": "module",
  "main": "server.js",
  "homepage": "https://github.com/dealpop/puppeteer-scraper#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/dealpop/puppeteer-scraper.git"
  },
  "keywords": [
    "price-tracking",
    "web-scraping",
    "playwright",
    "notifications",
    "e-commerce",
    "dealpop",
    "price-alerts",
    "automation"
  ],
  "author": "DealPop Development Team",
  "license": "MIT",
  "scripts": {
    "preinstall": "echo '=== PRE-INSTALL DEBUG ===' && echo 'Node version:' && node --version && echo 'NPM version:' && npm --version && echo 'Working directory:' && pwd && echo 'Environment:' && env | grep -E '(NODE|NPM|PATH)' && echo '=== END PRE-INSTALL ==='",
    "postinstall": "echo '=== POST-INSTALL DEBUG ===' && echo 'Checking Playwright:' && npm list playwright || echo 'Playwright check failed' && echo 'Node modules size:' && du -sh node_modules && echo '=== END POST-INSTALL ==='",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "test": "echo 'No tests specified' && exit 0",
    "test:scraper": "node price-scraper/scraper.js",
    "test:scraper:all": "node -e \"import('./price-scraper/scraper.js').then(m => m.testScraper())\"",
    "setup-db": "node -e \"import('./db.js').then(m => m.createTables())\"",
    "seed:db": "node -e \"import('./db.js').then(m => m.seed())\"",
    "health": "curl http://localhost:3000/health",
    "trigger": "curl -X POST http://localhost:3000/trigger",
    "docs": "echo '📚 Documentation available in ./docs/ directory' && ls -la docs/"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "pg": "^8.11.0",
    "playwright": "^1.56.1"
  },
  "optionalDependencies": {
    "bullmq": "^3.12.0",
    "ioredis": "^5.3.2",
    "redis": "^4.6.5",
    "twilio": "^4.19.0"
  },
  "devDependencies": {
    "eslint": "^8.38.0",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
